# Multi-region PC binary (one binary, any ROM, runtime US/EU/JP)

Goal: a single `tmc_pc` that loads a USA, EU, or JP ROM and plays it *faithfully* —
correct data, assets, text, and version-specific gameplay behavior — selected at
runtime from the loaded ROM. Replaces the previous one-binary-per-region model.

## Status

| Milestone | State |
|---|---|
| **M1 region foundation** (`include/region.h` dual-mode macros, `gActiveRegion`) | **DONE, verified** |
| **M2 `--multi_region` build mode** (compile once, runtime offset/region select) | **DONE** — one binary boots USA+EU+JP, each with correct offsets + area tables |
| **Matching maps (keystone)** | **DONE** — upstream `zeldaret/tmc` builds all 3 regions byte-matching; retail per-region maps in `tools/retail_maps/` (gitignored) |
| **M3 per-region script-func table** | **DONE** — `port_script_funcs.c` has runtime-selected USA/EU/JP tables (exact symbol-lookup derivation; USA/JP regression-clean) |
| **M3 GBA_script_\* + inline_ptrs** | **DONE** — `port_script_addrs.c` (generated by `tools/gen_script_addrs.py`) holds all 102 USA/EU/JP script-address triples; `Port_TranslateScriptAddr()` remaps each baked USA script address (`PORT_SCRIPT()`, ENTITY_SCRIPT storage) to the active region at resolve time. `port_inline_ptrs.c` (generated by `tools/gen_inline_ptrs.py`) patches the 9 interleaved rail pointers per region (`kInlinePtrs_USA/EU/JP`, runtime-selected). Both derived by exact symbol lookup in the retail maps; committed `fa76cf7d` / `8a8c92d3`. |
| **M3 EU init crash** | **DONE** — was an integer-overflow in the asset extractor's `read_pointer` bounds check (`offset+4` wrapped) on a misparsed EU text root; added overflow guard + count sanity caps in `assets_extractor.hpp`. **All 3 regions now boot to AgbMain in one binary.** |
| **M4 convert behavior `#ifdef`s to runtime** | **Control-flow + data-table phases DONE.** Simple-tail function slice (`ff61caaa`, `042be0a4`). Control-flow conversions + fn-dispatch + case-restructure (`bcfeaef7`). **Data-table phase complete** (`30f59e6d`, `a3f4e8da`, `89d9638d`, `1fba0688`, `d066aea5`, `06dc1a7b`): every per-region static-data table the non-baseline region observes now runtime-selects — `objectDefinitions.c` (51), `npcDefinitions.c`, `enemy.c`, `projectile.c`, `sound.c` gSongTable, `collision.c`, `gameData.c` (gWorldEvents), `transitions.c`, `droptables.c`, `itemMetaData.c`, `fileselect.c`, `screenTransitions.c`, `objPalettes.c`, `figurine*`, boss thresholds, lantern animPriority, etc. Each build + all-3-boot verified, with a repo-wide reader sweep confirming every consumer selects by region. See **M4 data-table conversion** below for the include-twice idiom. **REMAINING:** a few **exclusive-function dispatch** sites (`vram.c` EU `LoadFixedGFX`, `phonograph.c` `sub_0806E964`/`_USA`) — un-guard + runtime-dispatch. DEMO_* and debug-name/baseline-safe sites left alone. |
| **`flags.h` LocalFlags1 ordinal divergence** | **DONE for same-region play** — generated baseline-to-EU/JP tables in `port/flag_remap_generated.c` and the `*B` helpers in `src/flags.c` remap compiled C flag ordinals into the loaded ROM's native flag space. ROM/script ordinals remain native and untouched. The focused generator test verifies the divergent bank. |
| **M5 region-isolated saves** | **DONE** — `port_save.c` gives each region its own default save store (USA→`tmc.sav` back-compat, EU→`tmc_eu.sav`, JP→`tmc_jp.sav`), resolved lazily at first EEPROM load (after region detection). Angel SP4 has its own variant-specific filename. Switching ROM profiles no longer wipes or reinterprets another profile's data. Named profiles (`config.json`) stay region-agnostic; single-region builds are unchanged. This is deliberately **not** cross-region save portability. |
| **M5 profile-isolated asset cache** | **DONE** — the extracted runtime cache (and editable `assets_src/`) is keyed by exact ROM profile: `assets/usa/`, `assets/eu/`, `assets/jp/`, and `assets/jp-angel-sp4/`, each with its own `.asset_build_state.json`. Previously a single shared `assets/` folder was disambiguated only by a `{rom_size, rom_mtime}` fingerprint, but the retail and patched ROMs are 16 MB so the size collides and the buffer-load path stamps mtime `0` ("no opinion"). `Port_GetAssetCacheSubdir()` now drives the bootstrap, loader, and standalone extractor. USA keeps a legacy-flat `assets/` fallback (read-only); other profiles cannot pick up that tree. `sounds.json` stays profile-agnostic at the top level. |
| **M7 ship the fat binary as default** | **DONE** — `xmake` `multi_region` now defaults ON (dev builds match the shipped artifact; `--multi_region=n` / `build.py --single-region` keep the per-region guard build). CI (`_build.yaml`): new **multi-region boot gate** boots the fat binary headless against USA+EU(+JP when the assets repo has it) and requires `Entering AgbMain` per region; PPU parity now also runs JP; the optional JP baserom is copied when present. Release artifacts renamed `tmc-usa-*` → **`tmc-multi-*`** (the binary has been fat since v0.7.0 — the name now says so). EU golden hashes regenerated post fade-LUT fix (EU now hashes identical to USA for the content-identical boot scenes; stale goldens had the palette corruption baked in). INSTALL.md updated. |
| **M8 cross-region save portability** | TODO — the runtime remap now makes compiled USA-baseline C references agree with each active ROM's native save layout, but it does not migrate an existing save between those layouts. Portable saves still need an explicit, versioned flag/item/progression converter plus exhaustive verification; M5 therefore keeps saves isolated. |
| **M6 multi-language text verify + selector** | **Text VERIFIED; EU palette bug ROOT-CAUSED + FIXED.** Per-region `gTranslations` + fonts load with no NULL/fallback warnings; selector exists (`fileselect.c`, EU EN/FR/DE/ES/IT). USA + JP titles render perfectly. The EU garble was TWO stacked bugs, both fixed: (1) `Port_MakeFadeBuff256` (src/fade.c) read the 9-table fade/brightness LUT block at the USA/JP offset `0xF84`, but EU's block sits at `0xFCC` (+0x48 code shift; content byte-identical in all three ROMs) — every faded palette row on EU went through garbage LUTs, tinting ALL EU rendering; now region-shifted. (2) the extracted EU palette cache slices palette files +0x10 (half a palette) off, so `Port_LoadPaletteGroupFromAssets` served shifted rows — EU palette groups now fall through to the always-correct ROM path (`gGlobalGfxAndPalettes[palId*32]`), same shape as the JP gfx-group gate. EU title verified pixel-correct vs USA (frames 300/600). REMAINING: interactive pass through the EU language selector (FR/DE/ES/IT in-game text). |

## The keystone: matching retail maps (DONE)

Per-region **code** addresses (script-func keys, version-exclusive functions) can't be
content-anchored (code differs across versions) and this fork is intentionally
non-matching. Solved by building the **upstream `zeldaret/tmc`** decomp, which byte-matches:

```
git clone --depth 1 https://github.com/zeldaret/tmc /tmp/tmc_upstream
cd /tmp/tmc_upstream && ln -sfn /home/sian/tmc/tools/agbcc tools/agbcc
cp -L /home/sian/tmc/baserom.gba baserom.gba; cp /home/sian/tmc/baserom_eu.gba .; cp /home/sian/tmc/baserom_jp.gba .
CMAKE_POLICY_VERSION_MINIMUM=3.5 make usa eu jp -j$(nproc)   # all three print "tmc*.gba: OK" (sha1 match)
```

Maps copied to `tools/retail_maps/{usa,eu,jp}.map`. Derivation is then an **exact symbol
lookup**: e.g. a script-func entry names `sub_08046078`; that symbol is at `0x08046078`
(USA) / `0x08045e84` (EU) / `0x08045f74` (JP) in the three maps. Validated: the 470 USA
keys equal `usa_map_addr|1` with **0 mismatches**. `/tmp/derive_script_funcs.py` +
`/tmp/gen_script_funcs_mr.py` did the script-func generation; same method applies to the
remaining tables (`script_*` and rail-data symbols are all in the maps).

## Build & run

```
xmake f --game_version=USA --multi_region=y -y    # USA = asset/offset baseline
xmake build -y tmc_pc
```
Run against any supported ROM (`baserom.gba` / `baserom_eu.gba` /
`baserom_jp.gba`, or the exact Angel SP4 patch at runtime). The binary prints
`Active region set to {USA,EU,JP}` and selects `kRomOffsets_{USA,EU,JP}`;
clean JP and Angel SP4 both use the JP gameplay offsets, while SP4 additionally
selects its profile-specific text/font/cache settings.
`--multi_region=n` disables the default fat-binary mode and keeps the classic
single-region build. The matching GBA
ROM build is unaffected (`region.h` is pure compile-time constants there).

## Architecture

- **`include/region.h`** — the keystone. `REGION_IS_USA/EU/JP` are **compile-time
  constants** chosen by `-DUSA/-DEU/-DJP` in the default/matching build (so the decomp
  still byte-matches and dead branches fold away), but read the runtime **`gActiveRegion`**
  under `PC_PORT + MULTI_REGION`. Force-included by the build (`add_forceincludes`), so
  available everywhere; inert (macros only) in the non-multi-region path.
- **`gActiveRegion`** (`port_rom.c`) — set from `gRomRegion` in `Port_DetectRomRegion`,
  which already selects the right `kRomOffsets_*` table. Assets self-extract from the
  loaded ROM, so they follow the ROM with no bundling.

## M4 conversion pattern (for the 232 behavior sites)

Convert each region `#ifdef` from preprocessor to C control flow:

```c
#ifdef EU                         if (REGION_IS_EU) {
    eu_behavior();                    eu_behavior();
#else                       ->    } else {
    usa_behavior();                   usa_behavior();
#endif                            }

#if defined(JP) || defined(EU)  ->  if (REGION_IS_JP || REGION_IS_EU)
#ifndef EU                      ->  if (!REGION_IS_EU)
```

Rules:
1. In `MULTI_REGION` **both branches compile**, so any symbol a non-active branch
   references must exist for all regions. The ~3–5 version-exclusive functions
   (`AreaAllowsWarp` USA-only, `sub_08052878`/`sub_0805289C`, region-specific data
   arrays) must get un-`#ifdef`'d definitions (rename collisions, dispatch at runtime).
2. **The matching ROM build must keep working** — before converting `src/` sites, add
   `-include region.h` to the `xmake rom` task's preprocess (like the `-I port` fix), so
   converted files compile compile-time-constant there too.
3. No struct-layout differs by region (verified), so runtime branching is safe.
4. Verify each region at runtime with all three ROMs (USA/EU/JP all present locally).

Inventory: ~232 code-behavior sites across 47 files (mostly EU; JP ≈ USA with ~60 sites).
See the analysis in `docs/speedrun-and-rando-port-notes-2026-06-13.md` lineage.

## M4 data-table conversion (per-region static data)

Control-flow conversion does **not** work for static `const` data: a region `#ifdef`
inside a table initializer can't become a runtime `if`. The fat binary compiles only the
USA/JP baseline arm, so a loaded EU/JP ROM observes wrong data. Three mechanisms, pick the
lightest that fits — always gate new machinery on `MULTI_REGION` (not `PC_PORT`, which is
defined for all PC builds; the `_eu` twins and force-included `region.h` only exist under
`multi_region`):

1. **runtime-override** — single diverging field read at one/few consumers (esp. `const`):
   leave the table as-is (baseline), override at the reader:
   ```c
   x = table[i].field;
   #ifdef MULTI_REGION
   if (REGION_IS_EU) x = <eu value>;   // match the ORIGINAL guard's region set
   #endif
   ```
2. **twin-select** — a small whole table diverges: keep canonical = baseline, add an `_eu`
   (and `_jp` if the guard distinguishes JP) twin under `#ifdef MULTI_REGION`; each reader
   defaults to baseline then overrides under `if (REGION_IS_EU)`. Native EU stays correct via
   `#if defined(EU) && !defined(MULTI_REGION)` on the canonical body.
3. **include-twice** — a large/interleaved table (master + sub-tables + `MULTI_FORM`, e.g.
   `objectDefinitions.c`, `npcDefinitions.c`, `itemMetaData.c`): extract the data section to
   a `*_data.inc`, compile it twice with a `NAME(x)` decoration (`x` then `x##_eu`) and an
   `*_EU_PASS` knob that selects EU bodies. Canonical pass → baseline symbols; `MULTI_REGION`
   pass → `_eu` twins. The sole consumer selects `gFoo_eu` at runtime. Preserves positional
   correctness automatically (no hand-computed array indices) and is immune to
   partial-conversion drift.

**Discipline that caught real bugs:** after converting, `nm -S build/pc/tmc_pc | grep <sym>`
to confirm baseline and `_eu` both exist and differ, then **grep the whole repo for every
twinned base symbol** and confirm each reader is region-selected (or justified baseline-safe).
Agents repeatedly missed in-file consumers and once assigned a twin unconditionally
(`sel = foo_eu` with no `REGION_IS_EU` guard → USA/JP read the EU table). Build + all-3-boot
is necessary but does **not** catch wrong-but-valid data — the reader sweep does.
